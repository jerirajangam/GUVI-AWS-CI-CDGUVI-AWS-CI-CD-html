version: 0.2

phases:
  build:
    commands:
      - echo "Building AWS HTML files"
      - cp -r * /tmp/build/
  post_build:
    commands:
      - echo "Build completed"
artifacts:
  files:
    - '**/*'
  discard-paths: yes
